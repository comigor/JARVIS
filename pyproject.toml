[project]
name = "openaiconversationenhanced"
version = "0.1.0"
description = "Add your description here"
authors = [
    { name = "Igor Borges", email = "igor@borges.dev" }
]
dependencies = [
    "homeassistant>=2023.10.5",
    "openai>=0.28.1",
]
readme = "README.md"
requires-python = ">= 3.11"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.rye]
managed = true
dev-dependencies = []
